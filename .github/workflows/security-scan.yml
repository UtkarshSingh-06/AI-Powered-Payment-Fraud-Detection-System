name: Security Scan

on:
  push:
    branches: [main, master]
  pull_request:
  schedule:
    - cron: '0 3 * * 1'

jobs:
  trivy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build images for scan
        run: |
          docker build -f backend/Dockerfile -t fraudshield/backend:scan .
          docker build -f services/inference-fastapi/Dockerfile -t fraudshield/inference:scan .
      - name: Trivy backend
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          image-ref: fraudshield/backend:scan
          format: sarif
          output: trivy-backend.sarif
          severity: CRITICAL,HIGH
          exit-code: '0'
      - name: Trivy inference
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          image-ref: fraudshield/inference:scan
          format: sarif
          output: trivy-inference.sarif
          severity: CRITICAL,HIGH
          exit-code: '0'

  codeql:
    runs-on: ubuntu-latest
    permissions:
      security-events: write
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: github/codeql-action/init@v3
        with:
          languages: javascript
      - uses: github/codeql-action/analyze@v3
