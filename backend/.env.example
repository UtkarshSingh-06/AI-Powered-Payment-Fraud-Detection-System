# Backend Environment Variables
# Copy this file to .env and update with your actual values

# Server Configuration
PORT=5000
NODE_ENV=development

# JWT Configuration (REQUIRED - Change in production!)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-min-32-chars
JWT_EXPIRES_IN=7d

# CORS Configuration (comma-separated origins; Vite dev default is http://localhost:3002)
CORS_ORIGIN=http://localhost:3002,http://localhost:3000

# Database Configuration (if using external database)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/fraudshield
PG_SSL=false
REDIS_URL=redis://localhost:6379
RABBITMQ_URL=amqp://guest:guest@localhost:5672
KAFKA_BROKERS=localhost:9092
KAFKA_CLIENT_ID=fraudshield-backend
DEFAULT_TENANT_ID=default
FIELD_ENCRYPTION_KEY=change-me-32-characters-minimum-key

# WebSocket Configuration
WS_PORT=5000
WS_REQUIRE_AUTH=true
INFERENCE_URL=http://localhost:8000
CASE_SERVICE_URL=http://localhost:5004
GATEWAY_INTERNAL_SECRET=your-super-secret-jwt-key-change-this-in-production-min-32-chars
AML_SERVICE_URL=http://localhost:5007
INFERENCE_TIMEOUT_MS=40
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_SUCCESS_URL=
STRIPE_CANCEL_URL=
SEED_ON_START=false
SERVE_FRONTEND=false
MFA_DEV_BYPASS=false
MFA_REQUIRED_ROLES=admin,super_admin,analyst
ACCESS_TOKEN_EXPIRES_IN=15m
REFRESH_TOKEN_EXPIRES_IN=7d
COOKIE_SECURE=false
COOKIE_SAME_SITE=lax
