# Azure deployment stub

Planned resources: AKS, Azure Database for PostgreSQL, Azure Cache for Redis, Event Hubs (Kafka API).

Use `infra/terraform/aws` as the reference implementation; mirror interfaces in `infra/terraform/modules/*`.
