# GitHub Actions Notes

Primary workflow is located at `.github/workflows/ci-cd.yml`.

This folder stores architecture notes and reusable snippets for future split workflows:

- `build-and-test.yml`
- `security-scan.yml`
- `deploy-staging.yml`
- `deploy-production.yml`
